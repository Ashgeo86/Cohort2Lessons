/**
 * STRING = string
 * Convert string one to int 1
 * Convert int 1 to string one
 * Convert int 1 to a string. Is the result as expected?
 * Convert lowercase string to uppercase
 * Convert first letter of string to uppercase
 */

"STRING" == "string"
"STRING" > "string"
"STRING" < "string"
val one = "1".toInt
val x = 42
  x.toString
val result = "Ash Loves Scala".toUpperCase()
val result = "ash loves scala".capitalize